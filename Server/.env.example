# That closes out the full backend build:

# Auth & Users
# Vehicles
# Ride Templates & Instances (one-off + recurring + cron materialization)
# Search & Matching (per-leg seat logic)
# Pricing engine
# Bookings (transaction-safe, race-condition-proof)
# Cancellations (rider fee tiers + driver full-refund cascade)
# Notifications